# 📐 Workflow Diagrams - MetaStore

## 🔄 1. File Upload Workflow (Chunked)

```
┌─────────────────────────────────────────────────────────────┐
│                    FILE UPLOAD WORKFLOW                      │
└─────────────────────────────────────────────────────────────┘

┌──────────┐
│  User    │
│ (Browser)│
└────┬─────┘
     │
     │ 1. Select file (100MB video)
     │    Frontend: Chunk into 20 x 5MB pieces
     ▼
┌─────────────────────────────┐
│  Frontend Chunking Service  │
│  - Split file               │
│  - Create chunk queue       │
└────┬────────────────────────┘
     │
     │ 2. POST /api/v1/files/chunked-upload/initiate
     │    { name, path, size, mimeType, visibility }
     ▼
┌─────────────────────────────┐
│     Backend API             │
│  FilesService               │
│  - Create file record       │
│  - Initiate MinIO multipart │
│  - Generate presigned URLs  │
└────┬────────────────────────┘
     │
     │ 3. Return { fileId, uploadId, uploadUrls[] }
     ▼
┌─────────────────────────────┐
│     Frontend Upload         │
│  - Upload 3 chunks parallel │
│  - Track progress           │
│  - Retry on failure         │
└────┬────────────────────────┘
     │
     │ 4. PUT {uploadUrls[i]} with chunk data
     │    Receive ETag for each chunk
     │
     │ 5. Repeat until all chunks uploaded
     │    Progress: 15% → 30% → 45% → ... → 100%
     │
     │ 6. POST /api/v1/files/chunked-upload/complete
     │    { fileId, uploadId, parts: [{partNumber, etag}] }
     ▼
┌─────────────────────────────┐
│     Backend Processing      │
│  - Complete multipart       │
│  - Merge chunks in MinIO   │
│  - Update file status       │
└────┬────────────────────────┘
     │
     │ 7. If video → Trigger HLS processing
     ▼
┌─────────────────────────────┐
│  Media Processing Service   │
│  - Extract video segments  │
│  - Generate .m3u8 playlist  │
│  - Store in MinIO          │
└────┬────────────────────────┘
     │
     │ 8. Create audit log
     │    Send notification via WebSocket
     ▼
┌──────────┐
│ Complete │
│ File ready│
└──────────┘
```

---

## 🔗 2. Share Link Workflow

```
┌─────────────────────────────────────────────────────────────┐
│                    SHARE LINK WORKFLOW                      │
└─────────────────────────────────────────────────────────────┘

┌──────────┐
│  Owner   │
│  (User)  │
└────┬─────┘
     │
     │ 1. Select file/folder to share
     │    POST /api/v1/share-links
     │    { resourceId, permission, expiresAt?, password? }
     ▼
┌─────────────────────────────┐
│  ShareLinksService          │
│  - Generate unique token     │
│  - Create share link record │
│  - Set permissions           │
└────┬────────────────────────┘
     │
     │ 2. Return { id, token, expiresAt, ... }
     │
     │ 3. Owner copies link: /share/[token]
     │    Shares with recipient
     ▼
┌──────────┐
│Recipient │
│(Public)  │
└────┬─────┘
     │
     │ 4. Access share link
     │    GET /share/[token]
     ▼
┌─────────────────────────────┐
│  Public Share Page          │
│  - Display file info        │
│  - Password prompt (if set)  │
└────┬────────────────────────┘
     │
     │ 5. POST /api/v1/share-links/token/:token/access
     │    { password? }
     ▼
┌─────────────────────────────┐
│  Backend Validation          │
│  - Check active status      │
│  - Verify expiry date       │
│  - Validate password        │
│  - Increment access count   │
└────┬────────────────────────┘
     │
     │ 6. Return presigned download URL
     │    (or streaming URL for video)
     ▼
┌──────────┐
│Recipient │
│Downloads │
│/Views    │
└──────────┘
```

---

## 💳 3. Payment & Subscription Workflow

```
┌─────────────────────────────────────────────────────────────┐
│              PAYMENT & SUBSCRIPTION WORKFLOW                 │
└─────────────────────────────────────────────────────────────┘

┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Select subscription plan
     │    (FREE / PLUS / PRO)
     │    POST /api/v1/payments/subscriptions
     │    { plan, paymentMethod }
     ▼
┌─────────────────────────────┐
│  PaymentsService            │
│  - Create subscription       │
│  - Status: PENDING           │
│  - Generate QR code          │
│  - Notify admins             │
└────┬────────────────────────┘
     │
     │ 2. Return { subscription, qrCode, paymentInfo }
     ▼
┌──────────┐
│  User    │
│  - Scan QR│
│  - Transfer money            │
│  - Upload proof (optional)   │
└────┬─────┘
     │
     │ 3. Admin reviews payment
     │    GET /api/v1/payments/pending
     ▼
┌─────────────────────────────┐
│  Admin Panel                │
│  - View pending payments    │
│  - Review proof             │
└────┬────────────────────────┘
     │
     │ 4. Approve payment
     │    POST /api/v1/payments/:id/approve
     ▼
┌─────────────────────────────┐
│  PaymentsService            │
│  - Update status: COMPLETED │
│  - Upgrade storage quota    │
│  - Set expiry date          │
│  - Notify user              │
└────┬────────────────────────┘
     │
     │ 5. WebSocket notification
     │    + DB notification
     ▼
┌──────────┐
│  User    │
│  Subscription│
│  Activated   │
└──────────┘
```

---

## 🔔 4. Notification Workflow

```
┌─────────────────────────────────────────────────────────────┐
│                  NOTIFICATION WORKFLOW                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────┐
│  Event Occurs               │
│  - File approved/rejected   │
│  - Payment approved         │
│  - Share link accessed      │
└────┬────────────────────────┘
     │
     │ 1. NotificationsService.createAndDispatch()
     ▼
┌─────────────────────────────┐
│  Save to Database           │
│  Notification entity         │
│  - userId                    │
│  - type                      │
│  - message                   │
│  - payload                   │
└────┬────────────────────────┘
     │
     │ 2. NotificationsGateway.emitToUser()
     │    Find user's active WebSocket connections
     ▼
┌─────────────────────────────┐
│  WebSocket Server           │
│  (Socket.IO)                │
│  - Emit 'notification' event│
│  - Payload: notification data│
└────┬────────────────────────┘
     │
     │ 3. Real-time delivery to client
     ▼
┌─────────────────────────────┐
│  Frontend Client           │
│  - Listen to WebSocket     │
│  - Display notification     │
│  - Mark as read            │
└─────────────────────────────┘
```

---

## 🔐 5. Authentication Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  AUTHENTICATION WORKFLOW                    │
└─────────────────────────────────────────────────────────────┘

┌──────────┐
│  Client  │
│ (Browser)│
└────┬─────┘
     │
     │ 1. POST /api/v1/auth/login
     │    { username, password }
     ▼
┌─────────────────────────────┐
│  AuthService                │
│  - Verify credentials       │
│  - Check Argon2 hash        │
│  - Validate user status     │
└────┬────────────────────────┘
     │
     │ 2. Query database
     ▼
┌─────────────────────────────┐
│  Database                   │
│  User entity                │
└────┬────────────────────────┘
     │
     │ 3. Generate JWT tokens
     │    - Access token (15 min)
     │    - Refresh token (7 days)
     ▼
┌─────────────────────────────┐
│  Response                   │
│  - Set httpOnly cookies     │
│  - Return user info         │
└────┬────────────────────────┘
     │
     │ 4. Client stores cookies
     │    Auto-include in requests
     ▼
┌──────────┐
│Authenticated│
│  Client     │
└──────────┘
     │
     │ 5. On token expiry
     │    POST /api/v1/auth/refresh
     ▼
┌─────────────────────────────┐
│  Refresh Token              │
│  - Validate refresh token  │
│  - Generate new access token│
└─────────────────────────────┘
```

---

## 📦 6. Outbox Pattern (Event Publishing)

```
┌─────────────────────────────────────────────────────────────┐
│                  OUTBOX PATTERN WORKFLOW                   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────┐
│  Application Service        │
│  (e.g., FilesService)       │
└────┬────────────────────────┘
     │
     │ 1. Business transaction
     │    - Save file to database
     │    - Create outbox event
     │    (Same database transaction)
     ▼
┌─────────────────────────────┐
│  Database Transaction       │
│  - File record              │
│  - Outbox event record      │
│  (Atomic commit)            │
└────┬────────────────────────┘
     │
     │ 2. Outbox Poller
     │    (Background worker)
     │    - Poll outbox table
     │    - Find unpublished events
     ▼
┌─────────────────────────────┐
│  Outbox Events Table       │
│  - id                       │
│  - event_type               │
│  - payload                  │
│  - status (pending/published)│
│  - published_at             │
└────┬────────────────────────┘
     │
     │ 3. Publish to message broker
     │    - RabbitMQ / Kafka
     │    - Mark as published
     ▼
┌─────────────────────────────┐
│  Message Broker            │
│  (RabbitMQ)                 │
│  - Topic exchange           │
│  - Durable queues           │
└────┬────────────────────────┘
     │
     │ 4. Event consumers
     │    - Indexing service
     │    - Notification service
     │    - Analytics service
     ▼
┌─────────────────────────────┐
│  Event Consumers            │
│  - Process events           │
│  - Idempotency check        │
│  - Acknowledge on success   │
└─────────────────────────────┘
```

---

## 🔄 7. Saga Pattern (Multi-step Transaction)

```
┌─────────────────────────────────────────────────────────────┐
│              SAGA PATTERN - FILE UPLOAD SAGA                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────┐
│  Step 1: Initiate Upload    │
│  - Create file record       │
│  - Create outbox event      │
└────┬────────────────────────┘
     │
     │ ✅ Success
     ▼
┌─────────────────────────────┐
│  Step 2: Upload to Storage │
│  - MinIO multipart upload   │
│  - Upload chunks            │
└────┬────────────────────────┘
     │
     │ ✅ Success
     ▼
┌─────────────────────────────┐
│  Step 3: Process Media      │
│  - HLS conversion           │
│  - Thumbnail generation     │
└────┬────────────────────────┘
     │
     │ ✅ Success
     ▼
┌─────────────────────────────┐
│  Step 4: Send Notification │
│  - Create notification      │
│  - Emit via WebSocket      │
└─────────────────────────────┘

     │
     │ ❌ Failure at any step
     ▼
┌─────────────────────────────┐
│  Compensation Logic         │
│  - Rollback file record     │
│  - Delete from MinIO        │
│  - Cancel processing        │
│  - Send error notification  │
└─────────────────────────────┘
```

---

## 🏗️ 8. System Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    SYSTEM ARCHITECTURE                      │
└─────────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │   Client     │
                    │  (Browser)   │
                    └──────┬───────┘
                           │
                           │ HTTP/WebSocket
                           ▼
        ┌──────────────────────────────────┐
        │      API Gateway / Load Balancer  │
        └──────────────┬───────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
┌──────────────┐            ┌──────────────┐
│   Frontend   │            │   Backend     │
│  (Next.js)   │            │  (NestJS)     │
└──────────────┘            └──────┬───────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
            ┌──────────┐    ┌──────────┐    ┌──────────┐
            │Database  │    │  MinIO   │    │  Redis   │
            │(Postgres)│    │(Storage) │    │ (Cache)  │
            └──────────┘    └──────────┘    └──────────┘
                                   │
                                   ▼
                            ┌──────────┐
                            │RabbitMQ  │
                            │(Events)  │
                            └──────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
            ┌──────────┐    ┌──────────┐    ┌──────────┐
            │Indexing  │    │Media     │    │Analytics │
            │Service   │    │Processor │    │Service   │
            └──────────┘    └──────────┘    └──────────┘
```

---

## 🔄 9. API Versioning Strategy

```
┌─────────────────────────────────────────────────────────────┐
│                    API VERSIONING                           │
└─────────────────────────────────────────────────────────────┘

/api/v1/auth/login          → Current version
/api/v1/files/upload        → Current version
/api/v1/share-links         → Current version

/api/v2/auth/login          → Future version
/api/v2/files/upload        → Future version
                              (with breaking changes)

/api/v1/auth/login          → Still supported
                              (deprecated, will be removed in v3)
```

**Versioning Rules:**
- URL path versioning (`/v1`, `/v2`)
- Backward compatibility maintained
- Deprecation warnings in headers
- Migration guide provided

---

## 📊 10. Data Flow - Complete System

```
┌─────────────────────────────────────────────────────────────┐
│                    COMPLETE DATA FLOW                       │
└─────────────────────────────────────────────────────────────┘

User Action
    │
    ├─→ Frontend (React)
    │       │
    │       ├─→ API Client
    │       │       │
    │       │       └─→ Backend API (NestJS)
    │       │               │
    │       │               ├─→ Service Layer
    │       │               │       │
    │       │               │       ├─→ Database (PostgreSQL)
    │       │               │       │
    │       │               │       ├─→ Storage (MinIO)
    │       │               │       │
    │       │               │       └─→ Outbox Table
    │       │               │               │
    │       │               │               └─→ Message Broker
    │       │               │                       │
    │       │               │                       └─→ Event Consumers
    │       │               │                               │
    │       │               │                               ├─→ Notifications
    │       │               │                               ├─→ Indexing
    │       │               │                               └─→ Analytics
    │       │               │
    │       │               └─→ WebSocket Gateway
    │       │                       │
    │       │                       └─→ Real-time Updates
    │       │
    │       └─→ State Management (Zustand)
    │
    └─→ Response
            │
            └─→ UI Update
```

---

## 🎯 Key Takeaways

1. **Chunked Upload**: Giải quyết vấn đề file lớn
2. **Outbox Pattern**: Đảm bảo reliable event publishing
3. **Saga Pattern**: Xử lý multi-step transactions
4. **WebSocket**: Real-time notifications
5. **Idempotency**: Xử lý duplicate requests
6. **API Versioning**: Backward compatibility

---

**Note**: Các diagrams này có thể được sử dụng trong:
- Presentation slides
- Technical documentation
- Architecture reviews
- Onboarding materials

